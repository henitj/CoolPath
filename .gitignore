# ── Python (backend) ────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.coverage
coverage/
htmlcov/

# runtime state: SQLite hazard DB + cached live grids (both regenerated)
backend/data/

# ── Node / builds ───────────────────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.tsbuildinfo

# ── Expo / React Native ─────────────────────────────────────────────
.expo/
.exo/
web-build/
expo-env.d.ts
.metro-health-check*
# generated by `expo prebuild` / `eas build`
mobile/ios/
mobile/android/

# ── Secrets & local config ──────────────────────────────────────────
.env
.env.*
!.env.example
*.local

# ── Editors / OS / logs ─────────────────────────────────────────────
.vscode/
.idea/
*.sw?
.DS_Store
*.log
npm-debug.log*
yarn-error.log*
